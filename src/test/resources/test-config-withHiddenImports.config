// Json config file with comments unquoted field names

{
    root-dir: "example root dir",
    modules: [
        {  name: "AAA", path: "path/one"},
        {  name: "BBB", path: "path/two",
           hide-import: ["hide.package1.Class1", "hide.package2.Class2"]
        }
    ]
}
