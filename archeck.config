// ArCheck default config file (a json file with comments and unquoted field names)

{
    report-dir: "build/reports/archeck",
    modules: [
        {name: "archeck", path: "src/main/java"}
    ]
}
