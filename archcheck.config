// ArchCheck default config file (a json file with comments and unquoted field names)

{
    report-dir: "build/reports/archcheck",
    modules: [
        {name: "archcheck", path: "src/main/java"}
    ]
}
